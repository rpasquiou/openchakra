const computeTableMap = async (eventId) => {

  //récupérer les tables
  
  //récupérer les voeux
  //si voeu présent à 1 seule table -> affecté à la table
  
  //on commence par ambdassaeur
  //puis parrains
  //puis contributeurs
    //on fait -> pour chaque voeu -> on l'attribue au partner (avec des places restantes) avec le moins de voeux restants
    //on vérifie -> si partners avec écart de voeux attribué > 2 -> ? (trouver moyen d'égaliser)
  
  //on remplit les places libres avec les gens qui restent (contraintes sur le remplissage ?)
}